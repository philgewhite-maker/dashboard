// Fill this in after creating an OAuth client in Google Cloud Console (see
// README.md -> Google Drive sync setup). It's the Client ID from the
// Credentials page, ending in ".apps.googleusercontent.com" — not a secret,
// safe to commit to this repo (client-side OAuth apps have no client
// secret at all; that's the point of this flow).
const GOOGLE_CLIENT_ID = '315498494220-nknscrcep5113fol3h9bragvv55rhi3i.apps.googleusercontent.com';

export { GOOGLE_CLIENT_ID };
